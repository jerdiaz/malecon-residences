/**
 * Capa global de grano de película + viñeta. Estática y sin interacción
 * (pointer-events: none). Aporta la textura editorial "cara" sobre el fondo.
 */
export default function GrainOverlay() {
  return <div className="grain-overlay" aria-hidden />;
}
